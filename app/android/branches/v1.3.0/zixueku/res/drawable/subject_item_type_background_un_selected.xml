<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle" >

    <!-- 填充色 -->
    <solid android:color="@color/white" />
    <!-- 线的宽度，颜色 -->
    <stroke
        android:width="@dimen/button_stroke_width"
        android:color="@color/grey" />
   

    <size
        android:height="@dimen/subject_item_type_background_height"
        android:width="@dimen/subject_item_type_background_width" />

    <corners android:radius="@dimen/subject_item_type_background_radius" />

</shape>